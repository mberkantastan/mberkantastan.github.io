---
const { kicker, title, text } = Astro.props;
---
<div class="mb-8 max-w-3xl">
  <p class="kicker mb-3">{kicker}</p>
  <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">{title}</h2>
  {text && <p class="mt-4 text-base leading-7 text-slate-400">{text}</p>}
</div>
